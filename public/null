use App\Models\Materia;
$materia->formulas->count()
use App\Models\Materia;
$materia->formulas->count()
use App\Models\Materia;
$materia=Materia::find(2);
$materia->formulas->count()
use App\Models\Materia;
$materia=Materia::find(2);
count($materia->formulas)
use App\Models\Materia;
$materia=Materia::find(2);
count($materia->temas->)
use App\Models\Materia;
$materia=Materia::find(2);
count($materia->tema->)
use App\Models\Materia;
$materia=Materia::find(2);
count($materia->tema)
use App\Models\Materia;
$materia=Materia::find(2);
count($materia->temas)
use App\Models\Materia;
$materia=Materia::find(4);
count($materia->temas)
